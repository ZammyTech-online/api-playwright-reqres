# Base URL for the ReqRes API (can be overridden per environment)
BASE_URL=https://reqres.in

# API key required by the assignment. Do NOT commit real keys.
# In CI (GitHub Actions), store this as a repository secret named X_API_KEY.
X_API_KEY=REPLACE_ME